# Optional. Leave unset to use the Vite dev proxy (npm run dev) or same-origin
# hosting. For a separately hosted backend, set the full origin here AND enable
# CORS on the backend for this frontend's origin.
#
# VITE_API_BASE=https://api.your-ledger.example.com
